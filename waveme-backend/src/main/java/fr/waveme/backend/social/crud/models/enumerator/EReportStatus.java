package fr.waveme.backend.social.crud.models.enumerator;

/**
 * Enum representing the status of a report.
 * Each status indicates the current state of the report in the system.
 */
public enum EReportStatus {
    PENDING,
    ACCEPTED,
    REJECTED,
    ARCHIVED
}
